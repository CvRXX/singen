#What is singen?
Singen is  a wave generator that creates .wav files so it could be used with a normal soundcard. Singen also has support for noise and shifted waves.

#How to use it
To use singen, just call the singen_cli.py file and a default config will be generated. Edit this config file to your own needs to change the generated file.
